version: '3.8'

services:
  classifier:
    build: .
    container_name: ai_request_classifier
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./settings:/app/settings
      - ./completed:/app/completed
      - ./input_requests.csv:/app/input_requests.csv
